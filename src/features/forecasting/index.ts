/**
 * Feature: Forecasting
 * Phase 0 stub.
 * Phase 2: Integrate domain engine forecasting with DB queries.
 *
 * RULE: Forecast calculations are deterministic — see src/domain/index.ts.
 * AI may assist with narrative generation (describing the forecast)
 * but NEVER with the arithmetic itself.
 */

export type { StockoutForecast } from "../../domain/index";
